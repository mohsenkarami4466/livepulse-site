# 📊 وابستگی‌های شاخص‌های 12 تایی (IndicatorsContainer)

**تاریخ بررسی:** 2025-12-06

---

## 📁 فایل‌های اصلی

### 1. کامپوننت React
- **مسیر:** `src/components/Indicators/IndicatorsContainer.jsx`
- **نوع:** React Functional Component
- **وظیفه:** نمایش 12 شاخص مالی در دو ردیف (6+6)

### 2. استایل CSS
- **مسیر:** `src/components/Indicators/IndicatorsContainer.css`
- **نوع:** CSS Module
- **وظیفه:** استایل‌های شاخص‌ها (glassmorphism, responsive, theme support)

---

## 🔗 وابستگی‌های React

### 1. استفاده در Layout
- **فایل:** `src/components/Layout/Layout.jsx`
- **خط:** 174
- **نوع:** Import و استفاده مستقیم
```jsx
import IndicatorsContainer from '../Indicators/IndicatorsContainer'
// ...
<IndicatorsContainer />
```

---

## 🎯 وابستگی‌های DOM

### 1. Header (هدر)
- **هدف:** محاسبه موقعیت top
- **روش دسترسی:**
  - `document.querySelector('.glass-header, .header-container')`
  - `document.querySelector('header')`
- **استفاده:** محاسبه `headerHeight` برای `top: ${headerHeight + 8}px`

### 2. GlobeClock (کره کوچک)
- **هدف:** محاسبه موقعیت left و ارتفاع
- **ID:** `globeClockWrapper`
- **روش دسترسی:** `document.getElementById('globeClockWrapper')`
- **استفاده:**
  - محاسبه `globeWidth`, `globeHeight`, `globeLeft`
  - تنظیم `left` و `right` کارت شاخص‌ها
  - تنظیم `height` کارت شاخص‌ها (برابر با ارتفاع کره)

---

## 🆔 ID های خاص

### شاخص‌های اصلی (ردیف اول)
1. `goldIndicator` - طلا
2. `usdIndicator` - دلار
3. `eurIndicator` - یورو
4. `btcIndicator` - بیت‌کوین
5. `oilIndicator` - نفت
6. `nasdaqIndicator` - نزدک

**نکته:** این ID ها در HTML تعریف شده‌اند اما **فعلاً با static data** پر می‌شوند (بدون update داینامیک)

---

## 🎨 کلاس‌های CSS

### کلاس‌های اصلی
- `.indicators-glass-card` - کارت شیشه‌ای اصلی
- `.indicators-unified-container` - container داخلی (grid layout)
- `.indicator-item` - هر شاخص اصلی (طلا، دلار، ...)
- `.pair-item` - هر جفت ارز (EUR/USD, ...)

### کلاس‌های وضعیت
- `.up` - برای شاخص‌های صعودی (رنگ سبز)
- `.down` - برای شاخص‌های نزولی (رنگ قرمز)

### کلاس‌های داخلی
- `.indicator-icon` - آیکون شاخص
- `.indicator-name` - نام شاخص
- `.indicator-value` - مقدار شاخص
- `.indicator-change` - تغییرات شاخص
- `.pair-name` - نام جفت ارز
- `.pair-value` - مقدار جفت ارز
- `.pair-change` - تغییرات جفت ارز

---

## 📦 وابستگی‌های CSS خارجی

### 1. style.css (فایل اصلی)
- **مسیر:** `style.css` و `public/style.css`
- **خطوط:** 6200-6466
- **نوع:** استایل‌های قدیمی که توسط `IndicatorsContainer.css` override می‌شوند
- **شامل:**
  - `.pair-item.up` / `.pair-item.down`
  - `.indicators-unified-container` (responsive styles)
  - `.indicator-item`, `.pair-item` (responsive styles)

---

## 🔧 وابستگی‌های JavaScript

### 1. window Object
- **استفاده:** محاسبه موقعیت و اندازه
- **توابع استفاده شده:**
  - `window.addEventListener('resize', ...)` - برای responsive
  - `window.innerWidth` - برای responsive breakpoints

### 2. useEffect Hooks
- **useEffect 1:** محاسبه و تنظیم موقعیت کارت
  - وابسته به: Header, GlobeClock
  - اجرا می‌شود: بعد از mount و resize
  - تاخیرها: 100ms, 500ms, 1000ms (برای اطمینان از render شدن)

---

## 📊 ساختار داده

### شاخص‌های ردیف اول (6 عدد)
1. **طلا** - 🥇 - `goldIndicator` - `+۰.۵%`
2. **دلار** - 💵 - `usdIndicator` - `+۰.۳%`
3. **یورو** - 💶 - `eurIndicator` - `-۰.۲%`
4. **بیت‌کوین** - ₿ - `btcIndicator` - `+۲.۵%`
5. **نفت** - 🛢️ - `oilIndicator` - `-۱.۲%`
6. **نزدک** - 📈 - `nasdaqIndicator` - `+۰.۹٪`

### جفت ارزهای ردیف دوم (6 عدد)
1. **EUR/USD** - `1.0856` - `+0.12%`
2. **USD/JPY** - `149.82` - `-0.08%`
3. **GBP/USD** - `1.2715` - `+0.15%`
4. **USD/CHF** - `0.8842` - `-0.05%`
5. **AUD/USD** - `0.6548` - `+0.22%`
6. **USD/CAD** - `1.3625` - `-0.10%`

**نکته مهم:** همه داده‌ها **static** هستند و از API یا source خارجی update نمی‌شوند.

---

## 🎯 موقعیت و Layout

### موقعیت
- **Position:** `fixed`
- **Top:** `calc(var(--header-height, 60px) + 8px)`
- **Left:** محاسبه شده بر اساس `globeClockWrapper` + gap
- **Right:** `8px` (برابر با فاصله کره از چپ)
- **Height:** برابر با ارتفاع `globeClockWrapper`
- **Z-index:** `998` (زیر GlobeClock که 999 است)

### Layout
- **Grid:** `grid-template-columns: repeat(6, 1fr)`
- **Rows:** `grid-template-rows: repeat(2, 1fr)`
- **Gap:** `clamp(2px, 0.5vw, 6px)`
- **Padding:** `clamp(4px, 0.8vw, 10px)`

---

## 📱 Responsive Breakpoints

1. **Desktop Large (1400px+):**
   - Height: `180px`
   - Gap: `8px`
   - Padding: `12px`

2. **Desktop (1024px - 1399px):**
   - Height: `clamp(120px, 12vw, 160px)`
   - Gap: `clamp(4px, 0.7vw, 6px)`
   - Padding: `clamp(6px, 1vw, 10px)`

3. **Tablet (768px - 1023px):**
   - Height: `clamp(80px, 10vw, 120px)`
   - Gap: `clamp(3px, 0.6vw, 5px)`
   - Padding: `clamp(4px, 0.8vw, 8px)`

4. **Mobile Large (480px - 767px):**
   - Height: `clamp(60px, 8vw, 100px)`
   - Gap: `2px`
   - Padding: `3px`

5. **Mobile Small (320px - 479px):**
   - Height: `clamp(50px, 7vw, 80px)`
   - Gap: `1px`
   - Padding: `2px`

---

## 🎨 Theme Support

### Dark Theme (پیش‌فرض)
- Background: `rgba(18, 18, 18, 0.6)`
- Backdrop-filter: `blur(20px) saturate(150%)`
- Text color: `rgba(255, 255, 255, 0.9)`

### Light Theme
- Background: `rgba(255, 255, 255, 0.6)`
- Backdrop-filter: `blur(20px) saturate(150%)`
- Text color: `rgba(15, 23, 42, 0.9)`
- Box-shadow: `0 4px 24px rgba(0, 0, 0, 0.06)`

---

## ⚠️ نکات مهم

1. **Static Data:** همه مقادیر static هستند و update نمی‌شوند
2. **Position Dependency:** وابسته به Header و GlobeClock برای محاسبه موقعیت
3. **No API:** هیچ API call یا data fetching وجود ندارد
4. **Override CSS:** استایل‌های قدیمی در `style.css` override می‌شوند
5. **React Only:** کاملاً در React مدیریت می‌شود (بدون vanilla JS dependency)

---

## 🔄 توابع و متدهای استفاده شده

### در IndicatorsContainer.jsx
- `React.useRef()` - برای containerRef
- `React.useEffect()` - برای محاسبه موقعیت
- `document.querySelector()` - برای پیدا کردن Header
- `document.getElementById()` - برای پیدا کردن GlobeClock
- `window.addEventListener()` - برای resize event
- `setTimeout()` - برای تاخیر در محاسبه موقعیت

---

## 📝 خلاصه

**کل وابستگی‌ها:**
1. ✅ React (useRef, useEffect)
2. ✅ DOM (Header, GlobeClock)
3. ✅ CSS (IndicatorsContainer.css, style.css)
4. ✅ Layout.jsx (استفاده)
5. ❌ API یا Data Source (ندارد)
6. ❌ Vanilla JS Functions (ندارد)
7. ❌ State Management (ندارد)

**آماده برای تغییرات!** 🚀

