# ⚡ راهنمای اجرای سریع

**تاریخ:** 2025-12-06

---

## 🚀 روش سریع (توصیه می‌شود)

### استفاده از اسکریپت خودکار:

```bash
./start-dev.sh
```

این اسکریپت:
- ✅ سرور را خودکار راه‌اندازی می‌کند
- ✅ Firefox را خودکار باز می‌کند
- ✅ اگر سرور قبلاً اجرا باشد، فقط Firefox را باز می‌کند

---

## 📝 روش دستی

اگر می‌خواهید خودتان دستی انجام دهید:

### 1. اجرای سرور:
```bash
npm run dev
```

### 2. باز کردن Firefox:
در یک ترمینال جدید:
```bash
firefox http://localhost:5173
```

یا اگر پورت دیگری است:
```bash
firefox http://localhost:3002
```

---

## 🔍 پیدا کردن پورت سرور

اگر مطمئن نیستید سرور روی چه پورتی اجرا شده:

```bash
lsof -i -P -n | grep -E "vite|node" | grep LISTEN
```

یا:

```bash
netstat -tlnp | grep -E "vite|node"
```

---

## 🛑 توقف سرور

برای توقف سرور:

```bash
# پیدا کردن PID
ps aux | grep vite

# توقف با PID
kill <PID>
```

یا:

```bash
pkill -f vite
```

---

## 📊 مشاهده لاگ‌ها

اگر از اسکریپت استفاده می‌کنید:

```bash
tail -f /tmp/vite-dev.log
```

---

## 💡 نکات

1. **سرور در background اجرا می‌شود** - می‌توانید ترمینال را ببندید
2. **Firefox خودکار باز می‌شود** - نیازی به باز کردن دستی نیست
3. **تغییرات خودکار اعمال می‌شوند** - Hot Reload فعال است

---

**نکته:** اگر Firefox باز نشد، می‌توانید دستی URL را در مرورگر باز کنید.

