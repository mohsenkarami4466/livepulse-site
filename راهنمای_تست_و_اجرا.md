# 🚀 راهنمای تست و اجرای پروژه

**تاریخ:** 2025-12-06

---

## 📋 پیش‌نیازها

قبل از شروع، مطمئن شوید که Node.js و npm نصب شده‌اند:

```bash
node --version  # باید v16 یا بالاتر باشد
npm --version   # باید v8 یا بالاتر باشد
```

---

## 🎯 روش سریع تست (Live Server)

### 1️⃣ نصب وابستگی‌ها (فقط یک بار)

```bash
npm install
```

این دستور تمام وابستگی‌های پروژه (React, Vite, etc.) را نصب می‌کند.

---

### 2️⃣ اجرای سرور توسعه (Development Server)

```bash
npm run dev
```

این دستور:
- ✅ سرور توسعه Vite را راه‌اندازی می‌کند
- ✅ پروژه را در `http://localhost:5173` باز می‌کند (یا پورت دیگری)
- ✅ **Hot Module Replacement (HMR)** فعال است - یعنی هر تغییری که در کد می‌دهید، **بلافاصله** در مرورگر نمایش داده می‌شود! 🔥

**نکته:** بعد از اجرای این دستور، یک URL در ترمینال نمایش داده می‌شود. روی آن کلیک کنید یا در مرورگر باز کنید.

---

### 3️⃣ مشاهده تغییرات در لحظه

بعد از اجرای `npm run dev`:

1. **مرورگر را باز کنید** (معمولاً خودکار باز می‌شود)
2. **کد را ویرایش کنید** در VS Code یا هر ویرایشگر دیگر
3. **ذخیره کنید** (Ctrl+S یا Cmd+S)
4. **مرورگر خودکار به‌روز می‌شود!** ✨

**مثال:**
- فایل `src/pages/Home/Home.jsx` را باز کنید
- یک متن را تغییر دهید
- ذخیره کنید
- مرورگر بلافاصله تغییر را نشان می‌دهد!

---

## 🛠️ دستورات مفید دیگر

### Build برای Production

```bash
npm run build
```

این دستور:
- پروژه را برای production بهینه می‌کند
- فایل‌های نهایی را در فولدر `dist/` می‌سازد

### Preview Production Build

```bash
npm run preview
```

این دستور:
- نسخه production را در مرورگر نمایش می‌دهد
- برای تست قبل از deploy استفاده می‌شود

### Lint (بررسی خطاهای کد)

```bash
npm run lint
```

این دستور:
- خطاهای کد را پیدا می‌کند
- مشکلات style را نشان می‌دهد

---

## 🔍 دیباگ و عیب‌یابی

### مشکل: سرور راه‌اندازی نمی‌شود

**راه حل:**
1. مطمئن شوید که پورت 5173 آزاد است
2. اگر پورت اشغال است، Vite خودکار پورت دیگری انتخاب می‌کند
3. به URL نمایش داده شده در ترمینال توجه کنید

### مشکل: تغییرات نمایش داده نمی‌شوند

**راه حل:**
1. مرورگر را Refresh کنید (F5 یا Ctrl+R)
2. Cache مرورگر را پاک کنید (Ctrl+Shift+R)
3. مطمئن شوید که فایل را ذخیره کرده‌اید

### مشکل: خطا در Console مرورگر

**راه حل:**
1. Console مرورگر را باز کنید (F12)
2. خطا را بخوانید
3. معمولاً خطا می‌گوید کدام فایل و خط مشکل دارد

---

## 📱 تست در موبایل

برای تست در موبایل:

1. سرور را اجرا کنید: `npm run dev`
2. IP آدرس کامپیوتر خود را پیدا کنید:
   - Windows: `ipconfig` در CMD
   - Mac/Linux: `ifconfig` یا `ip addr`
3. در موبایل، به `http://YOUR_IP:5173` بروید

**نکته:** مطمئن شوید که کامپیوتر و موبایل در یک شبکه WiFi هستند.

---

## 🎨 نکات مهم

### Hot Module Replacement (HMR)

Vite از HMR استفاده می‌کند که یعنی:
- ✅ تغییرات CSS **فوراً** اعمال می‌شوند
- ✅ تغییرات React **بدون reload** اعمال می‌شوند
- ✅ State React حفظ می‌شود (مثلاً اگر یک Modal باز است، بعد از تغییر کد باز می‌ماند!)

### فایل‌های استاتیک

فایل‌های در فولدر `public/` مستقیماً قابل دسترسی هستند:
- `/data/countries-data.js` → `public/data/countries-data.js`
- `/globe/financial-globe.js` → `public/globe/financial-globe.js`

### فایل‌های React

فایل‌های در فولدر `src/` باید import شوند:
```javascript
import Component from './components/Component'
```

---

## 📝 چک‌لیست تست

قبل از commit یا deploy، این موارد را تست کنید:

- [ ] `npm run dev` بدون خطا اجرا می‌شود
- [ ] صفحه اصلی (Home) نمایش داده می‌شود
- [ ] Navigation کار می‌کند
- [ ] Modals باز و بسته می‌شوند
- [ ] Globes رندر می‌شوند
- [ ] Cards نمایش داده می‌شوند
- [ ] `npm run build` بدون خطا اجرا می‌شود

---

## 🆘 کمک بیشتر

اگر مشکلی پیش آمد:
1. Console مرورگر را بررسی کنید (F12)
2. Terminal را بررسی کنید (خطاهای سرور)
3. فایل‌های log را بررسی کنید

---

**نکته:** این راهنما برای استفاده روزمره است. برای اطلاعات بیشتر، به مستندات Vite مراجعه کنید: https://vitejs.dev/

