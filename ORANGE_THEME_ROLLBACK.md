# 🔄 راهنمای برگشت تم نارنجی به آبی

این فایل شامل تمام تغییراتی است که برای تبدیل تم از آبی به نارنجی انجام شده است.

## 📝 خلاصه تغییرات

### 1. متغیرهای CSS در `style.css`

#### تم روشن (`[data-theme="light"]`):
- `--accent-blue`: از `#2563eb` به `#ea580c` تغییر یافت
- `--currency-color`: از `#2563eb` به `#ea580c` تغییر یافت

#### تم تاریک (`[data-theme="dark"]`):
- `--accent-blue`: از `#60a5fa` به `#fb923c` تغییر یافت
- `--currency-color`: از `#60A5FA` به `#fb923c` تغییر یافت

### 2. رنگ‌های مستقیم در `style.css`

تمام موارد زیر از آبی به نارنجی تغییر یافتند:
- `rgba(59, 130, 246, ...)` → `rgba(234, 88, 12, ...)`
- `#3b82f6` → `#ea580c` (تم روشن) یا `#fb923c` (تم تاریک)
- `#2563eb` → `#c2410c` (نارنجی تیره)
- `#60a5fa` → `#fb923c` (نارنجی روشن)

### 3. فایل‌های React Components

#### `src/components/Layout/Layout.css`:
- سایه نئونی هایلایت‌ها: `rgba(59, 130, 246, ...)` → `rgba(234, 88, 12, ...)`

#### `src/components/Shared/GlobalSections.css`:
- `var(--accent-blue, #3b82f6)` → `var(--accent-blue, #ea580c)`
- `var(--accent-blue-dark, #2563eb)` → `var(--accent-blue-dark, #c2410c)`

## 🔙 دستورالعمل برگشت

برای برگشت به تم آبی، تمام کامنت‌های `/* تغییر به نارنجی (قبلاً: ...) */` را پیدا کنید و مقادیر قبلی را برگردانید.

یا می‌توانید از Find & Replace استفاده کنید:
- `#ea580c` → `#2563eb` (تم روشن)
- `#fb923c` → `#60a5fa` (تم تاریک)
- `#c2410c` → `#2563eb`
- `rgba(234, 88, 12,` → `rgba(59, 130, 246,`

## 📅 تاریخ تغییرات
تاریخ: 2025-12-06
وضعیت: تم نارنجی فعال

