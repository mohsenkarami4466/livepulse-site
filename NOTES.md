# یادداشت درخواست کاربر

## درخواست اصلی:
1. ✅ بررسی کامل تمام فولدرها و فایل‌های پروژه
2. ✅ بررسی تغییرات Git مخصوصاً تا دیشب ساعت ۳-۵ صبح
3. ✅ بررسی تمام موارد مربوط به استایل‌ها
4. ✅ بررسی باگ‌های مربوط به استایل و ابعاد در تبلت، گوشی و دسکتاپ
5. ✅ تهیه گزارش کامل از ساختار پروژه و کارکرد هر بخش

## وضعیت انجام کار:
- [x] ایجاد فایل یادداشت
- [x] بررسی ساختار کلی پروژه
- [x] بررسی تغییرات Git
- [x] بررسی فایل‌های استایل
- [x] بررسی باگ‌های responsive design
- [x] تهیه گزارش نهایی

## 📊 گزارش کامل پروژه

### ساختار کلی پروژه:
این یک پروژه React + Vite است که برای نمایش قیمت‌های لحظه‌ای ارز، طلا و رمزارز طراحی شده است.

**فولدرهای اصلی:**
- `src/` - کدهای React (کامپوننت‌ها، صفحات، router)
- `styles/` - فایل‌های CSS تقسیم‌بندی شده
- `globe/` - کدهای مربوط به کره‌های 3D
- `utils/` - توابع کمکی
- `data/` - داده‌های استاتیک
- `public/` - فایل‌های استاتیک
- `docs/` - فایل‌های build شده برای GitHub Pages
- `dist/` - فایل‌های build شده

### تغییرات Git (دیشب ۳-۵ صبح):
**تعداد کامیت‌ها:** 20+ کامیت
**فایل‌های تغییر یافته:** 
- فایل‌های CSS (sections.css, responsive.css, cards.css, globe.css)
- فایل‌های JavaScript (globe-clock.js, globe-3d.js)
- فایل‌های HTML (index.html, 404.html)
- vite.config.js

**مهم‌ترین تغییرات:**
1. حذف `style.css` قدیمی و تقسیم به فایل‌های component
2. اصلاح selectorهای CSS برای highlights
3. تغییرات در responsive design
4. رفع باگ‌های redeclaration در JavaScript

### فایل‌های استایل:
**تعداد فایل‌های CSS:** 78 فایل
**فایل‌های اصلی:**
- `styles/variables.css` - متغیرهای CSS
- `styles/responsive.css` - استایل‌های responsive
- `styles/components/sections.css` - استایل‌های بخش‌ها و highlights
- `styles/components/cards.css` - استایل‌های کارت‌ها
- `styles/components/globe.css` - استایل‌های کره‌ها
- `styles/components/modals.css` - استایل‌های مودال‌ها
- `styles/components/other.css` - استایل‌های دیگر
- `styles/themes-complete.css` - تم‌های کامل
- `styles/theme-optimization.css` - بهینه‌سازی تم

**تعداد Media Query:** 79 مورد در 6 فایل

### باگ‌های پیدا شده:

#### 1. مشکل Selector در sections.css:
- **مشکل:** استفاده از `.layout-main .highlights-section` به جای `.layout-main > .view .highlights-section`
- **تأثیر:** استایل‌ها ممکن است به درستی اعمال نشوند
- **فایل:** `styles/components/sections.css`

#### 2. مشکل Gap در highlights:
- **دسکتاپ:** gap: `+95px` (باید `+20px`)
- **تبلت:** gap: `-0px` (باید `+20px`)
- **موبایل:** gap: `+3px` (باید `+20px`)
- **فایل:** `styles/components/sections.css`

#### 3. مشکل JavaScript Override:
- **مشکل:** JavaScript در `globe-clock.js` با `!important` CSS را override می‌کند
- **مقادیر اشتباه:**
  - دسکتاپ: `+15px` (باید `+20px`)
  - موبایل: `+23px` (باید `+20px`)
  - fallback: `+5px` (باید `+20px`)
- **فایل:** `globe/globe-clock.js`

#### 4. مشکل Responsive در تبلت:
- **مشکل:** استایل‌های responsive برای تبلت (768px-1024px) ممکن است کامل نباشند
- **فایل:** `styles/components/sections.css`

#### 5. مشکل Cache:
- **مشکل:** ممکن است cache مرورگر باعث نمایش استایل‌های قدیمی شود
- **راه حل:** Hard refresh (Ctrl+Shift+R)

### ساختار کامپوننت‌های React:
- `App.jsx` - کامپوننت اصلی
- `Layout/` - Layout اصلی
- `Header/` - هدر
- `Cards/` - کارت‌های قیمت
- `Globes/` - کره‌های 3D
- `Indicators/` - شاخص‌های لحظه‌ای
- `Modals/` - مودال‌ها
- `Pages/` - صفحات (Home, News, Tools, Globe, Tutorial, Relax)

### وابستگی‌ها:
- React 18.2.0
- React Router DOM 6.20.0
- Vite 5.0.8
- Three.js (برای کره‌های 3D)
- D3.js (برای نقشه‌ها)
- GSAP (برای انیمیشن‌ها)

## ✅ خلاصه:
پروژه به خوبی ساختاردهی شده است اما چند باگ در responsive design و selectorهای CSS وجود دارد که باید رفع شوند.

---

## 🔧 تغییرات انجام شده (رفع باگ‌ها):

### 1. رفع Gap در highlights (sections.css):
- ✅ دسکتاپ: `-5px` → `+20px`
- ✅ تبلت: `-0px` → `+20px`
- ✅ موبایل (480px-767px): `+3px` → `+20px`
- ✅ موبایل کوچک (479px): `+3px` → `+20px`
- ✅ موبایل خیلی کوچک (390px): `+3px` → `+20px`
- ✅ موبایل خیلی خیلی کوچک (320px): `+3px` → `+20px`

### 2. رفع مشکل CSS Syntax (other.css):
- ✅ بستن `@media (min-width: 1025px)` که بدون `}` بسته نشده بود

### 3. بررسی JavaScript (globe-clock.js):
- ✅ مقادیر gap در JavaScript درست هستند (همه `+20px`)

### 4. بررسی Selectorها:
- ✅ Selectorها درست هستند: `.layout-main > .view .highlights-section`

**تاریخ رفع باگ‌ها:** ۱۴ دسامبر ۲۰۲۵

---

## 🔧 تغییرات اضافی (رفع خطای globeContainer):

### 4. رفع خطای "globeContainer پیدا نشد" (globe-clock.js و script-init.js):
- ✅ بهبود `checkReactMode()` برای تشخیص بهتر React mode (بررسی چندباره)
- ✅ اضافه کردن بررسی React mode در `initGlobe()` برای جلوگیری از فراخوانی در React mode
- ✅ اضافه کردن بررسی وجود `globeContainer` قبل از هر فراخوانی `initGlobe()`
- ✅ افزایش تاخیر از 1 ثانیه به 2 ثانیه برای render شدن React
- ✅ افزایش maxRetries از 30 به 50 (5 ثانیه)
- ✅ بررسی React mode در همه نقاط قبل از `initGlobe()`

**فایل‌های تغییر یافته:**
- `globe/globe-clock.js` - اضافه کردن بررسی React mode در ابتدای `initGlobe()`
- `script-init.js` - بهبود `checkReactMode()` و اضافه کردن بررسی‌های متعدد

### 5. رفع مشکل "کلا چیزی بالا نیومده" (App.jsx):
- ✅ تغییر `basename` در `BrowserRouter` از ثابت `/livepulse-site` به conditional
- ✅ در development: `basename="/"` (با استفاده از `import.meta.env.DEV`)
- ✅ در production: `basename="/livepulse-site/"`
- ✅ این مشکل باعث می‌شد که صفحه در development اصلاً لود نشود

**فایل‌های تغییر یافته:**
- `src/App.jsx` - تغییر basename به conditional بر اساس محیط
