# 📋 بررسی کامل نقشه 2D و وابستگی‌ها

## 🔍 فایل‌های مرتبط با نقشه 2D

### 1. فایل اصلی نقشه 2D
- **`gold-map.js`**: کلاس `WorldGoldMapGlass` - نقشه اصلی 2D
  - Container: `#goldMapGlass`
  - Event handler: `handleCountryClick()` - خط 752
  - مشکل: در خطوط 786-800 کد باز کردن کره 3D وجود دارد!

### 2. فایل‌های مرتبط
- **`globe-2d-maps.js`**: مدیریت نقشه‌های 2D در هایلایت‌های کره‌ها
  - تابع `createGlobe2DMap()`: ایجاد نقشه 2D برای هایلایت‌ها
  - تابع `openGlobe3DFrom2D()`: باز کردن کره 3D از نقشه 2D
  - تابع `returnTo2DFrom3D()`: بازگشت از کره 3D به نقشه 2D

### 3. فایل‌های React
- **`src/pages/Home/Home.jsx`**: صفحه اصلی که نقشه 2D را نمایش می‌دهد
  - Container: `#goldMapGlass` در خط 411
  - State: `showRanking` برای نمایش/پنهان رتبه‌بندی

### 4. فایل‌های استایل
- **`public/style.css`**: استایل‌های نقشه 2D
- **`style.css`**: استایل‌های اضافی

### 5. فایل‌های داده
- **`public/data/countries-data.js`**: داده‌های کشورها
- **`gold-map.js`**: داده‌های داخلی نقشه (تابع `buildCompleteData()`)

## 🐛 مشکل اصلی

### مشکل: کلیک روی کشور در نقشه 2D باعث باز شدن کره 3D می‌شود

**مکان مشکل**: `gold-map.js` خطوط 786-800

```javascript
// انتخاب کشور در کره منابع (اگر باز باشد)
if (typeof window.selectCountry === 'function') {
    window.selectCountry(code);
    
    // اگر کره منابع باز نیست، باز کردن آن
    const resourcesModal = document.getElementById('resourcesGlobeModal');
    if (resourcesModal && !resourcesModal.classList.contains('active')) {
        // باز کردن کره منابع
        if (typeof window.openResourcesGlobe === 'function') {
            window.openResourcesGlobe();
        }
    }
}
```

**علت**: این کد همیشه اجرا می‌شود و کره 3D را باز می‌کند حتی اگر کاربر فقط می‌خواهد کشور را در نقشه 2D انتخاب کند.

## ✅ راه حل

این کد باید:
1. فقط زمانی اجرا شود که کره 3D **قبلاً باز** باشد
2. یا باید کاملاً حذف شود اگر نقشه 2D مستقل است
3. یا باید یک flag اضافه شود که مشخص کند آیا نقشه 2D باید با کره 3D هماهنگ باشد یا نه

## 📝 بررسی‌های لازم

- [x] بررسی اینکه آیا نقشه 2D باید با کره 3D هماهنگ باشد
  - **نتیجه**: نقشه اصلی (در صفحه Home) نباید با کره 3D هماهنگ باشد
  - فقط نقشه‌های 2D در هایلایت‌های کره‌ها باید با کره 3D هماهنگ باشند
- [x] بررسی اینکه آیا `globeType` در نقشه 2D اصلی تنظیم شده است
  - **نتیجه**: در نقشه اصلی `globeType` تنظیم نمی‌شود (null است)
  - در نقشه‌های هایلایت‌ها `globeType` تنظیم می‌شود
- [x] بررسی اینکه آیا `window.selectCountry` باید در نقشه 2D اصلی فراخوانی شود
  - **نتیجه**: فقط اگر `globeType` تنظیم شده باشد و کره 3D قبلاً باز باشد
- [x] بررسی اینکه آیا `window.openResourcesGlobe` باید در نقشه 2D اصلی فراخوانی شود
  - **نتیجه**: نباید فراخوانی شود - این کد حذف شد

## ✅ تغییرات انجام شده

1. اضافه شدن `globeType` به constructor (پیش‌فرض: null)
2. اضافه شدن `containerId` به constructor (پیش‌فرض: 'goldMapGlass')
3. تغییر `createMap()` برای استفاده از `this.containerId`
4. تغییر `handleCountryClick()` برای بررسی `globeType` قبل از باز کردن کره 3D
5. حذف کد باز کردن خودکار کره 3D - فقط اگر کره قبلاً باز باشد، کشور انتخاب می‌شود

