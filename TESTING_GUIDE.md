# 🧪 راهنمای تست React Application

**تاریخ:** 2024-12-06  
**وضعیت:** ✅ React Migration تکمیل شد

---

## 🌐 لینک تست

**URL:** `http://localhost:3000`

**نکته:** اگر سرور در حال اجرا نیست، دستور زیر را اجرا کنید:
```bash
npm run dev
```

---

## ✅ چیزهایی که باید ببینید

### 1. صفحه اصلی (Home) - `/`

**Header (بالای صفحه):**
- ✅ لوگو "LP" و نام "LivePulse.ir" در سمت راست
- ✅ دکمه تغییر تم (🌙) و تمام صفحه (⛶) در سمت چپ
- ✅ دکمه "ورود" در سمت چپ

**Globe Clock (بالا سمت چپ):**
- ⚠️ **مشکل:** فقط هاله دیده می‌شود، کره کامل نیست (در `GLOBE_CLOCK_TODO.md` ثبت شد)
- باید یک کره کوچک با ساعت UTC اطرافش باشد

**Highlights Section (دایره‌های رنگی):**
- ✅ 7 دایره: خانه، رمزارز، ارز، طلا، فارکس، بورس، نفت
- ✅ دایره "خانه" باید active (highlighted) باشد

**Cards Section (کارت‌های قیمت):**
- ✅ 4 کارت قیمت:
  - دلار آمریکا (USD)
  - طلای ۱۸ عیار (GOLD)
  - بیت‌کوین (BTC)
  - شاخص بورس (TEDPIX)
- ✅ هر کارت باید شامل:
  - نام
  - قیمت
  - درصد تغییر (سبز/قرمز)
  - نمودار مینی SVG
  - زمان آخرین آپدیت

**Gold Map Section (نقشه منابع):**
- ✅ هدر با عنوان "🗺️ نقشه جهانی منابع"
- ✅ دو dropdown: فیلتر و سال
- ✅ دو بخش: نقشه و رنکینگ (ممکن است خالی باشند)

**Bottom Navigation (پایین صفحه):**
- ✅ 6 دکمه: خانه، اخبار، کره‌ها، آموزش، آرامش، ابزارها
- ✅ دکمه "خانه" باید active باشد

---

### 2. صفحه اخبار (News) - `/news`

**باید ببینید:**
- ✅ عنوان "📰 اخبار"
- ✅ متن "صفحه اخبار - در حال توسعه..."
- ✅ Header و Bottom Navigation (همانند صفحه اصلی)
- ✅ Globe Clock (همانند صفحه اصلی)

---

### 3. صفحه کره‌ها (Globe) - `/globe`

**باید ببینید:**
- ✅ عنوان "🌍 کره‌ها"
- ✅ متن "صفحه کره‌ها - در حال توسعه..."
- ✅ Header و Bottom Navigation
- ✅ Globe Clock

---

### 4. صفحه آموزش (Tutorial) - `/tutorial`

**باید ببینید:**
- ✅ عنوان "📚 آموزش"
- ✅ متن "صفحه آموزش - در حال توسعه..."
- ✅ Header و Bottom Navigation
- ✅ Globe Clock

---

### 5. صفحه آرامش (Relax) - `/relax`

**باید ببینید:**
- ✅ عنوان "🧘 آرامش"
- ✅ متن "صفحه آرامش - در حال توسعه..."
- ✅ Header و Bottom Navigation
- ✅ Globe Clock

---

### 6. صفحه ابزارها (Tools) - `/tools`

**باید ببینید:**
- ✅ Highlights Section (7 دایره برای ابزارها)
- ✅ دایره "صندوق" باید active باشد
- ✅ **Personal Fund Section:**
  - کارت ارزش کل سرمایه
  - لیست دارایی‌ها
  - فرم افزودن دارایی
- ✅ **Gold Tool Section:**
  - فرم با فیلدهای: وزن، عیار، اجرت
  - دکمه "🧮 محاسبه"
- ✅ **Currency Converter Section:**
  - فرم با فیلدهای: از، به، مبلغ
  - دکمه "🔄 تبدیل"
- ✅ **Diamond Tool Section:**
  - فیلد آپلود عکس
  - دکمه "🔍 آنالیز"
- ✅ Header و Bottom Navigation
- ✅ Globe Clock

---

## 🧪 تست عملکرد

### Navigation
1. ✅ کلیک روی هر دکمه در Bottom Navigation → باید به صفحه مربوطه برود
2. ✅ URL باید تغییر کند (مثلاً `/news`، `/globe`)
3. ✅ دکمه active باید تغییر کند (highlight شود)
4. ✅ محتوای صفحه باید تغییر کند

### Modals
1. ✅ کلیک روی دکمه "ورود" در Header → LoginModal باید باز شود
2. ✅ کلیک روی overlay (پس‌زمینه تیره) → Modal باید بسته شود
3. ✅ کلیک روی × → Modal باید بسته شود
4. ✅ کلیک روی یک کارت قیمت → PriceModal باید باز شود (اگر login شده باشید)

### Theme Toggle
1. ✅ کلیک روی دکمه 🌙 → تم باید تغییر کند (light/dark)
2. ✅ تغییر باید در تمام صفحه اعمال شود

### Highlights (Home Page)
1. ✅ کلیک روی هر دایره → باید active شود
2. ✅ دایره قبلی باید غیرفعال شود

### Tools (Tools Page)
1. ✅ کلیک روی هر دایره → بخش مربوطه باید نمایش داده شود
2. ✅ فرم‌ها باید قابل استفاده باشند

---

## ⚠️ مشکلات شناخته شده

### 1. Globe Clock
**مشکل:** فقط هاله اطراف کره دیده می‌شود، خود کره کامل نیست  
**وضعیت:** در `GLOBE_CLOCK_TODO.md` ثبت شد - بعداً حل می‌شود  
**تأثیر:** عملکردی ندارد، فقط نمایشی

### 2. Gold Map
**مشکل:** نقشه و رنکینگ ممکن است خالی باشند  
**وضعیت:** نیاز به integration با کد vanilla JS  
**تأثیر:** نمایشی

---

## ✅ چیزهایی که باید کار کنند

### قطعاً کار می‌کنند:
- ✅ Navigation بین صفحات
- ✅ Header و Bottom Navigation
- ✅ Theme Toggle
- ✅ Modals (Login, Subscription, Price)
- ✅ Cards نمایش داده می‌شوند
- ✅ Highlights در Home و Tools
- ✅ Forms در Tools page
- ✅ State Management (localStorage)
- ✅ Responsive Design

### ممکن است نیاز به integration داشته باشند:
- ⚠️ Globe Clock (مشکل visibility)
- ⚠️ Gold Map (نیاز به vanilla JS)
- ⚠️ Globe Modals (نیاز به vanilla JS functions)

---

## 🔍 بررسی Console

**باز کردن Console:**
- Firefox: `F12` یا `Ctrl+Shift+K`

**چیزهایی که نباید ببینید:**
- ❌ خطاهای قرمز (Errors)
- ❌ Warnings زیاد

**چیزهایی که ممکن است ببینید (OK است):**
- ✅ Info logs (اگر debug mode فعال باشد)
- ✅ Warnings جزئی (مثل missing images)

---

## 📝 خلاصه

**وضعیت کلی:** ✅ **باید کار کند**

**چیزهایی که باید ببینید:**
1. ✅ تمام 6 صفحه قابل دسترسی هستند
2. ✅ Navigation کار می‌کند
3. ✅ Modals باز و بسته می‌شوند
4. ✅ Cards نمایش داده می‌شوند
5. ✅ Theme toggle کار می‌کند
6. ✅ Forms در Tools page کار می‌کنند

**مشکلات:**
- ⚠️ Globe Clock (فقط نمایشی - بعداً حل می‌شود)

---

**لینک:** `http://localhost:3000` 🚀

