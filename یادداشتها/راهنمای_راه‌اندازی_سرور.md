# 🚀 راهنمای راه‌اندازی سرور

## ⚠️ مشکل: تداخل پورت‌ها

اگر صفحه لود نمی‌شود و سرور روی پورت 3001 بالا می‌آید:

### 🔧 راه‌حل:

1. **توقف تمام process های vite:**
```bash
pkill -f "vite"
pkill -f "node.*vite"
```

2. **آزاد کردن پورت‌ها:**
```bash
fuser -k 3000/tcp 3001/tcp
```

3. **پاک کردن cache:**
```bash
rm -rf node_modules/.vite
rm -rf dist
```

4. **راه‌اندازی مجدد:**
```bash
npm run dev
```

---

## ✅ تغییرات اعمال شده:

- `strictPort: true` در `vite.config.js` اضافه شد
  - اگر پورت 3000 اشغال باشد، خطا می‌دهد به جای استفاده از پورت دیگر
- `host: true` اضافه شد برای دسترسی از شبکه محلی

---

## 📊 وضعیت:

- ✅ تمام process های قبلی متوقف شدند
- ✅ پورت‌ها آزاد شدند
- ✅ Cache پاک شد
- ✅ Build موفق بود

---

## 🌐 لینک:

http://localhost:3000

---

**تاریخ:** 2025-12-10

